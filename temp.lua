local a = {x = 13}
a
.
x
(
23
)

	-- Causa do erro TK_EOS
	-- case TK_NAME: {  /*  label:: loop */
	--       if (luaX_lookahead(ls) == TK_DBCOLON){
	--         blocklabelstat(ls);
	--         break;  
	--       } else
	--         break;
	--     }

